{
  "scripts": {
    "release:auto": "node scripts/create-release.js",
    "migrate:full": "node scripts/ownership-migration-bot-v2.js",
    "clean:all": "node scripts/cleanup-old-data.js",
    "notify:owners": "node scripts/send-notification.js",
    "publish:all": "npm run migrate:full && npm run release:auto && npm run notify:owners"
  }
}