---
"@elevenlabs/client": minor
"@elevenlabs/react": minor
---

Add cancellable WebRTC session startup through an optional `signal`, and make React `endSession()` return a teardown promise that aborts an in-flight start immediately.
