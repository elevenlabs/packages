{
  "$schema": "https://turborepo.com/schema.json",
  "ui": "tui",
  "globalEnv": ["NODE_ENV"],
  "tasks": {
    "generate-version": {
      "inputs": ["package.json"],
      "outputs": ["src/version.ts"]
    },
    "build": {
      "dependsOn": ["^build", "^generate", "generate-version"],
      "inputs": [
        "$TURBO_DEFAULT$",
        ".env*",
        "src/**",
        "!src/**/*.test.*",
        "!src/**/*.spec.*"
      ],
      "outputs": ["dist/**", ".next/**", "!.next/cache/**", "generated/**"]
    },
    "test": {
      "dependsOn": ["^build"],
      "inputs": [
        "src/**",
        "**/*.test.*",
        "**/*.spec.*",
        "vitest.config.ts",
        "jest.config.*"
      ],
      "passThroughEnv": ["PLAYWRIGHT_*", "HOME", "CI"]
    },
    "lint": {
      "dependsOn": ["^build", "lint:es", "lint:prettier", "check-types"]
    },
    "lint:es": {
      "dependsOn": ["generate"],
      "inputs": ["src/**", ".eslintrc*", "eslint.config.*"]
    },
    "lint:prettier": {
      "dependsOn": ["generate"],
      "inputs": ["src/**", ".prettierrc*", "prettier.config.*"]
    },
    "check-types": {
      "dependsOn": ["^build", "generate"],
      "inputs": ["src/**", "tsconfig.json"]
    },
    "clean": {
      "cache": false
    },
    "dev": {
      "cache": false,
      "persistent": true,
      "dependsOn": ["generate-version"]
    },
    "generate": {
      "dependsOn": ["generate-version"],
      "inputs": ["schemas/**/*.yaml", "schemas/**/*.yml"],
      "outputs": ["generated/**"]
    }
  }
}
