name: Build

on:
  push:
    branches: [main, next]
  pull_request:

permissions:
  checks: write
  contents: write

jobs:
  run-linters:
    name: Build packages
    runs-on: ubuntu-latest

    steps:
      - name: Check out repo
        uses: actions/checkout@v6

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up node
        uses: actions/setup-node@v6
        with:
          node-version: lts/Krypton

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Check Expo dependency alignment
        run: pnpm --dir examples/react-native-expo exec expo install --check

      - name: Run build
        run: pnpm run build
